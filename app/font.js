import { Red_Hat_Mono,Sono } from 'next/font/google';
export const redhatmon = Red_Hat_Mono({
  subsets: ['latin'],
  display: 'swap',
});
export const sono = Sono({
  subsets: ['latin'],
  display: 'swap',
});
